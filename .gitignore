# Nested independent git checkouts — tracked in their own repos/remotes,
# not as submodules. See plan/README.md for why.
# No trailing slash: these are also valid as symlinks (a checkout kept
# elsewhere and linked in), and a trailing slash matches directories only.
/dbt-core
/dbt-sqlserver

# OS / editor cruft
.DS_Store
*.swp
